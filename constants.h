#pragma once

#define _USE_MATH_DEFINES


#include <math.h>

const float PI = (float) M_PI;

const int SCREEN_WIDTH = 78;
const int SCREEN_HEIGHT = 40;

const int THOR_SEGMENTS = 90;
const int CIRCLE_SEGMENTS = 90;
const float THOR_RADIUS = 5.0f;
const float CIRCLE_RADIUS = 2.0f;

const float ROTATE_X_ANGLE = 0.006f;
const float ROTATE_Z_ANGLE = 0.004f;

const float Z_RANGE = 20.f;
const float RENDER_SCALE = 0.2f;

#undef _USE_MATH_DEFINES
